package Multithreading;

/**
 * Thread class: represents a thread of execution
 * Code to be execute in a separate thread must be implemented as run() of runnable
 * There are 2 ways to create threads in java
    * Thread class: When every thread is working on different state (thread per state)
    * Runnable interface: when you want multiple threads working on common state4
 * Three priorities of thread
    * Main priority
    * Normal priority
    * Min priority
 * There is three method in object class of Threading
    *

 */

public class Demo {
}
